function Block(letter, rate, father, code){
  this.letter = letter;
  this.rate = rate;
  this.used = used;
  this.father = father;
  this.code = code;
  }
  function FindCode(block) {
  block.code = tree[block.father].coded + '1';
  }
  else{
  if (block.letter == tree [premIndex].letter){
  block.code = '1';
  }
  else {
  FindCode(tree[block.father]);
  block.code = tree[block.father].code + '0';
  }
  }
  }
  
